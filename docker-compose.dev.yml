version: "3"
services:
  express-server:
    build: 
      context: .
      args:
        NODE_ENV: development
    volumes:
      - .:/app
      # - /app/node_modules
    environment: 
      - NODE_ENV=development
      - MONGO_USER=username
      - MONGO_PASSWORD=password
    command: npm run dev